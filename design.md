# Botanica · 江浙沪自然主义花园植物数据库 Web App 设计方案

> 适用场景：园艺设计时，从一份植物清单里快速找到「适合这块地、能互相搭配」的植物。
> 运行方式：完全在你自己电脑上打开使用，不需要连网、不需要部署到服务器。
> 项目 / 应用名称：**Botanica**

---

## 一、这个工具是做什么的（一句话）

把你现成的 Excel 植物清单，变成一个**能看图、能搜索、能筛选、能「攒出」一套种植搭配方案**的本地网页应用。

原来 Excel 做不了的事，它都能做：

- 给每种植物配**照片**（春夏秋冬、花、叶、果实）；
- 按**日照、水分、高度、花期、花色、常绿/落叶**等条件**筛选**；
- 把选中的植物**放进一个「设计方案」里**，按自然主义花园的分层（骨架树 / 下木灌木 / 宿根 / 观赏草 / 球根 / 地被）组织起来；
- 一眼看出**花期是不是前后接得上**（这是自然主义花园「四季有景」的灵魂）。

---

## 二、先回答你的核心问题：React + Vite 够用、适合吗？

**结论：够用，而且是很适合的选择。** 但要先把话说清楚，避免误会：

> React + Vite 只是「画画用的画布和画笔」，它管的是**界面怎么显示、怎么交互**。
> 一套完整应用 = 界面 + 数据（植物资料）+ 图片，三块缺一不可。

用盖房子打比方：

| 部件 | 对应技术 | 解决什么问题 |
| --- | --- | --- |
| 图纸/数据库 | 结构化数据文件（如 `plants.json`，或以后用 SQLite） | 放那 200 条植物资料 |
| 照片墙 | 一个 `images` 本地文件夹 | 放每种植物的图片文件 |
| 画布和画笔 | **React + Vite** | 把数据和图片「画」成可浏览、可点击的界面 |
| 水电/门窗 | 少量配置、一个导入脚本 | 把 Excel 一键变成数据；让你不用每次手动改代码 |

### 为什么推荐「纯前端、零后端」

因为你只在本地用、一个人用，其实**根本不需要一台服务器**。最省事、也最适合零基础的方案是：

> **React + Vite 纯前端** + **一个植物数据文件** + **一个本地图片文件夹**。

好处：

1. 启动非常简单（`npm run dev` 就打开浏览器看到界面）；
2. 不用学数据库、不用学服务器、不用担心安全；
3. 改植物资料，改数据文件即可（或以后做个「编辑页面」在界面上直接改）；
4. 图片就放本地文件夹，离线也能看。

### 什么时候才需要加后端 / SQLite？

只有当你出现下面任意一种情况，才值得升级（**现在不用做**）：

- 想多人同时用、多人同时编辑（比如团队园艺工作室）；
- 想存几百上千条自己的养护笔记、大量种植方案、还希望能跨设备同步；
- 想在手机上也能用。

这些以后都能加。**第一版先做纯前端，是最稳的路径。**

### 一句话给你的答案

**React + Vite 完全够用，很适合你这种「本地单用户 + 要看得见摸得着」的需求。**
你可能需要额外知道的是：它需要搭配「数据文件」和「图片文件夹」一起用，而不是只装 React 就够了。

---

## 三、参考了哪些国外知名植物数据库（我们学它们的什么）

这些数据库不用照搬，重点学它们的**有用设计**：

| 数据库 | 我们借鉴的点 |
| --- | --- |
| **Missouri Botanical Garden · Plant Finder**（密苏里植物园） | 最经典的「左侧筛选 + 右侧列表」：按日照、水分、高度、冠幅、花期、花色、耐寒带筛选。**这是本工具筛选功能的模板。** |
| **RHS Find a Plant**（英国皇家园艺学会） | 权威的**学名与品种**检索，以及「去哪个苗圃能买到」。学名准确性要用它校。 |
| **Great Plant Picks**（美国西北部） | 「经过验证、可靠好养的植物」清单。对应你 Excel 里「非常强健、可靠」这类的**可靠度**标注。 |
| **Mount Cuba Center**（多恩·库卡植物中心多年生草花试种） | 用**实际试种评估**挑出「表现出色」的多年生品种，正好补充「它到底好不好养、值不值得用」。 |
| **KEW Plants of the World Online (POWO)**（英国皇家植物园） | 权威科属分类、学名是否规范。**和你 Excel 里的 APG IV 目/科对齐。** |
| **图片来源**：Wikimedia Commons / iNaturalist / GBIF | 这些平台有大量**免费或 CC 授权**的植物照片。自己种的植物也建议自己拍，最贴合本地。 |

---

## 四、界面长什么样（浅显版）

主页几个标签页（Tab）就够，不用复杂：

1. **总览**：9 个设计层的大卡片（常绿乔木、常绿灌木、落叶乔木、落叶灌木、喜阳宿根、耐阴宿根、观赏草、球根根茎、匍匐攀援）。点进去看该层清单。
2. **植物库**：左侧是筛选栏（日照 / 水分 / 高度 / 花期 / 花色 / 常绿落叶 / 耐阴 / 香花 / 可靠度），右侧是植物卡片列表（卡片上有缩略图、中文名、拉丁学名、几个关键标签）。
3. **植物详情**：点卡片进入，大图 + 多张照片（四季）+ 所有字段 + 养护要点 + 「加入设计」「加入收藏」按钮。
4. **我的设计**：把你选中的植物按设计层分好组，形成一套种植方案，可以存下来、导出。

交互参考：**Missouri Botanical Garden** 的筛选体验、**RHS** 的学名搜索、**Great Plant Picks** 的「可靠清单」。

---

## 五、数据模型：从 Excel 到数据库

### 5.1 Excel 原始字段

| 列 | 含义 | 例子 |
| --- | --- | --- |
| A | APG IV 属 | 槭属 |
| B | 种 | 鸡爪槭 |
| C | 栽培品种/亚种/变种 | “春雨” |
| D | 拉丁学名 | Acer palmatum 'Harusame' |
| E | APG IV 目 | 无患子目 |
| F | APG IV 科 | 无患子科 |
| G | 别名/备注 | — |
| H | 特性 | 平常，可靠 |
| 表名 | 设计层（分类） | 落叶乔木 |

### 5.2 Excel 的「坑」，恰恰是数据库要解决的

1. **续表写法**：很多品种行的「属/科/目」是空白的（靠上一行继承），数据库里必须**补全**，否则没法按科属筛选。
2. **学名有时只到「属」**（如 `Salvia`、`Laurus`），并没有到种 —— 说明这条是「整属都能用」，标注清楚很重要。
3. **有约 49 条没有拉丁学名**，需要标成「学名待核对」。
4. **完全没有图片、高度、冠幅、花期、花色、水分** —— 这些都要补充。
5. **「特性」是自由文本**，最好拆成结构化的「可靠度、日照、水分、季节看点」等字段，才能筛选。

### 5.3 数据库里每条植物应该长这样（简化示例）

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一编号 |
| `设计层` | 9 个分类之一（如「落叶乔木」） |
| `中文名` | 鸡爪槭 |
| `拉丁学名` | Acer palmatum 'Harusame' |
| `属` / `科` / `目` | 已补全 |
| `别名` | — |
| `常绿/落叶` | 落叶 |
| `高度` / `冠幅` | 3–5m / 3–4m |
| `日照` | 全日照 / 半日照 / 耐阴 |
| `水分需求` | 旱 / 中 / 湿 |
| `花期` | 春季 / 夏季 / 秋季 / 冬季 |
| `花色` | 粉 / 白 / 紫 / 黄… |
| `季节看点` | 春叶 / 秋色 / 冬果 / 香花 |
| `香气` | 有 / 无 |
| `可靠度` | 很强健 / 一般 / 需养护（从「特性」提炼） |
| `特性（原文）` | 保留原始描述 |
| `图片` | 一张或多张文件名 |
| `标签` | 便于搜索（如「自然主景」「林下」「画框树」） |

---

## 六、功能清单（贴合「搭配」这个目标）

### 基础功能（第 1 版就做）

- 按 9 个**设计层**浏览；
- **搜索**中文名 / 拉丁学名 / 别名；
- **筛选**：日照、水分、高度、花期、花色、常绿落叶、耐阴、香花、可靠度；
- **植物详情** + **多张图片**；
- **收藏**常用植物。

### 进阶功能（第 2 版做，是「搭配」的核心）

- **我的设计**：把选中的植物按设计层分组，形成一套种植方案；
- **花期衔接提示**：自动检查你选的植物里，春夏秋冬分别有没有看点，缺哪个季节就提醒你补；
- **异季互补 / 色彩组合**：自然主义花园里「秋天的观赏草」「春天的球根」「夏天的宿根」是怎么互相搭的；
- **数据管理页**：在界面上直接新增 / 编辑植物图鉴、上传本地图片；也可以重新一键导入 Excel。

---

## 七、技术架构（最浅显的版本）

### 推荐的纯前端结构

```
你的项目文件夹/
├─ index.html            ← 网页入口（Vite 自动生成）
├─ public/
│  └─ images/            ← 放植物图片（离线也能看）
│     ├─ acer-palmatum-harusame.jpg
│     └─ salvia.jpg ...
├─ src/
│  ├─ data/
│  │  └─ plants.json     ← 植物数据（由 Excel 导入生成）
│  ├─ components/        ← 界面小零件（搜索框、筛选栏、卡片、详情页…）
│  ├─ pages/             ← 页面（总览、植物库、详情、我的设计）
│  └─ App.jsx            ← 应用入口
├─ package.json          ← 依赖清单（Vite、React 等）
└─ vite.config.js        ← 工具配置
```

**用到的技术（都很好上手）：**

- **React**：负责界面和交互；
- **Vite**：开发工具，让你改一下界面浏览器立刻刷新，启动快；
- **Tailwind CSS**（可选）：快速做出好看、统一的界面，不用自己写一堆 CSS；
- **plants.json**：存放植物资料（启动时加载）；
- **localStorage**：在界面里存「我的收藏」「我的设计」，不麻烦、无服务器；
- **图片文件**：放 `public/images/`，程序直接按文件名读取。

### 这套架构简单到什么程度？

- 打开方式：`npm run dev`，浏览器自动弹出界面。
- 要加点新植物：把图片丢进 `images` 文件夹 + 在 `plants.json` 里加一行（或以后做个「编辑页」直接在界面加）。
- 完全不需要装数据库软件、不需要配服务器、不需要学后端语言。

---

## 八、开发路径（零基础友好，分 4 步，每步都能看到成果）

> 我会一步步带你、帮你写代码，你不用先学会编程。

**第 1 步：准备工具（约 30 分钟）**
在你的 Mac 上装好 **Node.js**（一个运行环境的软件）和 **VS Code**（写代码的软件）。
我教你用一条命令生成一个「Vite + React」项目，并打开看效果。

**第 2 步：把 Excel 变成数据（我写脚本）**
我会写一个导入脚本，自动读取你的 `适合江浙沪的自然主义花园植物目录.xlsx`，补全「属/科/目」、清理续表，输出 `plants.json`。

**第 3 步：做 4 个核心页面**
总览（9 大类卡片）→ 植物库（搜索 + 筛选 + 卡片列表）→ 详情页 + 图片 → 收藏。

**第 4 步：做「我的设计 / 搭配」**
把选中的植物按设计层分组，加上花期衔接提示，形成可保存、可导出的种植方案。

### 最小可用版本（MVP）范围建议

第一版**不要贪多**，只做：

- 9 大类浏览；
- 名称搜索；
- 按「日照 / 常绿落叶 / 设计层」筛选；
- 植物详情 + 配图；
- 收藏。

这个先跑通，再往上加筛选维度和「搭配」功能，最稳。

---

## 九、需要注意的地方（风险与建议）

1. **图片来源与授权**：优先用 Wikimedia Commons、iNaturalist、GBIF 这类明确标注授权（多为 CC）的图片；商用要看清授权；最好加上「图片来源」标注。自己拍的照片最省心、最贴合本地。
2. **学名准确性**：用 KEW POWO、RHS、中国植物志（iPlant / 中国植物图像库）核对，避免你的清单里有年代久远或变动的学名。
3. **数据量**：200 条非常合适，精度比数量重要。不要一开始就想要几千条。
4. **本地使用**：数据就放在你电脑上，别传到云端；想备份就复制整个文件夹。
5. **持续更新**：以后你把 `plants.json` 和 `images/` 一起打包，就是一个完整、可迁移的「植物数据库」。

---

## 十、下一步

如果你愿意，我可以直接帮你：

- ① 生成 Vite + React 项目骨架；
- ② 写一个把 Excel 转成 `plants.json` 的导入脚本；
- ③ 搭出「总览 + 植物库 + 详情 + 收藏」的第一个能跑的版本。

告诉我你想从哪一步开始，我们就动手。
